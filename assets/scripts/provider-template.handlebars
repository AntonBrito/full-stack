<div id="providers-div" data-id="{{dataId}}">
    <h3>{{given_name}}</h3>
    <p>{{family_name}}</p>
    <p>{{email}}</p>
    <p>{{specialty}}</p>
    <button type="button" class="providerEditButton" data-id="{{dataId}}" name="editButton">Edit</button>
    <button type="button" class="providerDeleteButton" data-id="{{dataId}}" name="deleteButton">Delete</button>
  </div>
